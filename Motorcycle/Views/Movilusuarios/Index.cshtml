@model IEnumerable<Motorcycle.Models.Movilusuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroMovilUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation.NombreUsuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroMovilUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.NombreUsuario)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdMovilUsuario">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdMovilUsuario">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdMovilUsuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
