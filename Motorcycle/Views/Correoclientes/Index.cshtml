@model IEnumerable<Motorcycle.Models.Correocliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CorreoCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdClienteNavigation.NombreCliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CorreoCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdClienteNavigation.NombreCliente)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdCorreoCliente">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdCorreoCliente">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdCorreoCliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
